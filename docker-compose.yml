services:
  api:
    build:
      context: .
      dockerfile: deploy/local.Dockerfile
    container_name: peep-ai-api
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.dev
    ports:
      - "8000:8000"
